server:
  host: 0.0.0.0
  port: 8080
  root: static
  index: index.html
  auto_index: true

# tls:
#   enabled: false
#   cert_path: ./tls/fullchain.pem
#   key_path: ./tls/privkey.pem

# routing:
#   - path: /
#     serve: static  # This is now the default - no need to specify if serving static at root
#   - path: /api/*
#     proxy:
#       url: https://backend.example.com
#       timeout: 5s
#       add_headers:
#         X-Forwarded-For: "{client_ip}"

# spa:
#   enabled: true
#   fallback: /index.html

# assets:
#   cache:
#     enabled: true
#     max_age: 7d
#     etag: false

# compression:
#   enable: true
#   gzip: true
#   br: true

# security:
# cors:
#   enabled: true
#   allowed_origins:
#     - https://app.example.com
#   allowed_methods:
#     - GET
#     - POST
#     - PUT
#     - DELETE
#     - OPTIONS

# rate_limit:
#   enabled: true
#   requests_per_min: 1

# headers:
#   Strict-Transport-Security: "max-age=63072000; includeSubDomains; preload"
#   X-Frame-Options: "DENY"
#   X-Content-Type-Options: "nosniff"

# obs:
#   level: info

